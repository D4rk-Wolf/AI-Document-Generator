# AI Template — Vite + React App

This directory contains the React application. See the [root README](../README.md) for full project documentation.

## Local Development

```bash
npm install
npm run dev       # http://localhost:5173
npm run build     # production build → dist/
npm run preview   # preview production build locally
npm run lint      # run ESLint
```
